services:
  node:
    build: .
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    expose:
      - 3000
    ports:
      - "3030:3000"
    volumes:
      - .:/home/node/app